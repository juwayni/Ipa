import{describe,it,expect}from'vitest';import{searchIPA}from'../domain/search';import{gradeReview}from'../domain/srs';import{ipaEntries}from'../data/ipa';
describe('IPA app domain',()=>{it('contains broad IPA inventory',()=>{expect(ipaEntries.length).toBeGreaterThan(100);expect(ipaEntries.some(e=>e.symbol==='ʃ')).toBe(true);expect(ipaEntries.some(e=>e.category==='diacritic')).toBe(true)});it('finds esh through sh alias and description',()=>{expect(searchIPA('sh')[0].symbol).toBe('ʃ');expect(searchIPA('voiceless postalveolar fricative').some(e=>e.symbol==='ʃ')).toBe(true)});it('updates spaced review state',()=>{const r=gradeReview(undefined,'good');expect(r.timesSeen).toBe(1);expect(r.mastery).toBeGreaterThan(0);expect(r.nextReview).toBeTruthy()})});
